{% set name = "appmap" %}
{% set version = "1.1.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/appmap-{{ version }}.tar.gz
  sha256: 9ee68478ad490d2dbac406bcbd5113e71886eb607dcceeb07a47206839e157a0

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - poetry
    - pip
    - python >=3.6,<4.0
  run:
    - importlib-metadata >=0.8
    - inflection >=0.3.0,<0.4.0
    - python >=3.6,<4.0
    - pyyaml >=5.3.0,<5.4.0

test:
  imports:
    - appmap
    - appmap._implementation
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/applandinc/appmap-python
  summary: Create AppMap files by recording a Python application.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ickc
